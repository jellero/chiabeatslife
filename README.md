# Chiabeatslife

Repository applicativo per la migrazione del sito `chiabeatslife.jacopoellero.it` da WordPress a Slim 4.

La ricostruzione mantiene i contenuti e gli asset visuali pubblici del sito esistente, elimina il runtime WordPress dal frontend e usa un front controller Slim 4 con route dichiarative e view PHP.
